import React from 'react';

const TimeItem = props => {
  return (
    <span className="timeItem" style={{display: 'inline-block'}}>
      Time Item from component
    </span>
  );
}

export default TimeItem;
